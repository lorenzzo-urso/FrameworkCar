# Configurações locais/pessoais do Claude Code (não compartilhar)
.claude/settings.local.json

# Scripts locais de inicialização (não commitar)
iniciar.bat
*.bat

# Pasta-fonte do design system (o runtime usado vive em poc/web/ds/)
Terra Comum Design System/

# Card generator — projeto independente, tem seu próprio git
card-generator/
