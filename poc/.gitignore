# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Node (caso web/ vire React no futuro)
node_modules/
dist/
.parcel-cache/

# Env / segredos — NUNCA commitar
.env
.env.*
*.local

# Dados geoespaciais grandes (tambem ignorados na raiz Dados/)
*.gpkg
*.tif
# OBS: .geojson NAO e ignorado — os samples pequenos (dados_demo/) sao
# necessarios para o codigo rodar. Arquivos grandes ficam em Dados/ (ja ignorado).

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
