# connectors/ — Dev B (bordas: dados externos). Python 3.11+
# Instalar num venv proprio:  python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

# --- B1: conector da consulta publica (situacao + geometria) ---
requests>=2.31          # chamada HTTP a consulta publica (quando o endpoint for confirmado)
# playwright>=1.42       # DESCOMENTAR so se a SPA exigir render de JS / for anti-bot
                        # (depois: python -m playwright install chromium)

# --- B2: recorte do GeoPackage SFB (geometria + cobertura) ---
geopandas>=1.0          # ler/recortar/reprojetar o GeoPackage SFB
pyogrio>=0.8            # leitura rapida do .gpkg por bbox (vem com o geopandas)
shapely>=2.0            # operacoes geometricas
